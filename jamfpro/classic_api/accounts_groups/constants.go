package accounts_groups

// Endpoint for the accounts groups Classic API.
// Note: Account groups share the /JSSResource/accounts endpoint with accounts,
// but use different path suffixes (/groupid/ and /groupname/).
const (
	EndpointClassicAccounts = "/JSSResource/accounts"
)
